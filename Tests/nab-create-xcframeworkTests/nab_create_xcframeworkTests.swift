import XCTest
@testable import nab_create_xcframework

final class nab_create_xcframeworkTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(nab_create_xcframework().text, "Hello, World!")
    }
}
